/*
Package xru (XyPriss Rule Unit) implements a Structured Text Patcher (STP) engine.

Unlike traditional patchers that rely on strict JSON round-tripping, XRU operates as a
Domain-Specific Language (DSL) for structured text transformation, preserving formatting,
comments, and non-standard syntaxes while applying complex mutations.

It is primarily used in the XyPriss ecosystem for template orchestration and
configuration management.
*/
package xru
